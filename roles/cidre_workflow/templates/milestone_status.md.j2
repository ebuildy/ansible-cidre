# Milestone {{ g_milestone.id }} - {{ g_milestone.title }}

## Open issues

{% for issue in g_status.content %}
{% if issue.milestone.id == milestone.id %}- \#{{ issue.id }} {{ issue.title }} - {{ issue.html_url | default(issue.web_url) }}{% endif %}
{% endfor %}
