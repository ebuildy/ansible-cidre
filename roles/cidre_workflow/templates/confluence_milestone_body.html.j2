{{ cidre_provider }} milestone: <a href="{{ cidre_milestone.web_url }}" title="open on {{ cidre_provider }}">{{ cidre_milestone.web_url }}</a>
{{ cidre_provider }} tag <a href="{{ tag_web_url }}" title="git tag on {{ cidre_provider }}">{{ tag_web_url}}</a>

{% if cidre_issue_provider %}

{% endif %}

<h2>Issues</h2>

<p>
  <ac:structured-macro
    ac:name="jira"
    ac:schema-version="1"
    ac:macro-id="35cfa126-c8c8-4310-bec6-33616fa42fa6">
    <ac:parameter ac:name="server">{{ cidre_confluence_jira_server }}</ac:parameter>
    <ac:parameter ac:name="columns">key,summary,type,created,updated,due,assignee,reporter,priority,status,resolution</ac:parameter>
    <ac:parameter ac:name="maximumIssues">20</ac:parameter>
    <ac:parameter ac:name="jqlQuery">project = {{ cidre_issue_repo }} AND fixVersion = {{ cidre_issue_component }}-{{ ms_version }} </ac:parameter>
    <ac:parameter ac:name="serverId">{{ cidre_confluence_jira_server_id }}</ac:parameter>
  </ac:structured-macro>
</p>

<h2> git changes</h2>

<ul>
{% for change in git_changes.stdout_lines %}
<li>{{ change }}</li>
{% endfor %}
</ul>

<br />
<br />

<small>Generated by https://cidre.io</small>
